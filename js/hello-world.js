'use strict';


/**
 *   Create a function that takes in a variable and outputs 'Hello, ' plus the variable
 *   For example: 'Hello, World!'
 **/

function greeting(name) {

    // Return 'Hello, ' concat '+' the variable 'name'
    return 'Hello, ' + name;

}

console.log(greeting("World"));